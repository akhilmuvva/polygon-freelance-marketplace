/**
 * useSocket: DECOMMISSIONED during Genesis Purge.
 * Absolute Zero Gravity achieved. 
 */
export const useSocket = () => {
    return { socket: null, isConnected: false, subscribeToEvents: () => {} };
};
